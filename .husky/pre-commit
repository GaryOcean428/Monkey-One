# Modern Husky v9+ format

# Exit on any error
set -e

echo "🔍 Running pre-commit checks..."

# Run lint-staged for formatting and linting
echo "📝 Running lint-staged..."
pnpm lint-staged

# Type checking (lenient mode for development)
echo "🔍 Type checking..."
pnpm run type-check || {
  echo "⚠️  TypeScript errors found. Consider running 'pnpm run type-check:strict' before production."
  echo "ℹ️  Continuing with commit (type errors are non-blocking in development)"
}

# Security audit (quick check) - non-blocking for development
echo "🔒 Security audit..."
pnpm audit --audit-level high --production || {
  echo "⚠️  Security vulnerabilities found. Run 'pnpm audit fix' to resolve."
  echo "ℹ️  Continuing with commit (audit failures are non-blocking in development)"
}

# Check for secrets in staged files (basic check)
echo "🔐 Checking for secrets..."
git diff --cached --name-only | xargs grep -l -E "(api_key|secret|password|token)" 2>/dev/null | grep -v ".env.example" | grep -v ".env.template" && {
  echo "⚠️  Potential secrets detected in staged files!"
  echo "Please review and remove any sensitive information before committing."
  echo "ℹ️  If these are test values or examples, consider adding them to .gitignore patterns"
} || true

echo "✅ Pre-commit checks passed!"
